import Service from '@ember/service';
import RouterService from '@ember/routing/router-service';
import RSVP from 'rsvp';
export default class LoadingService extends Service {
    router: RouterService;
    postDelay: number;
    preDelay: number;
    watchTransitions: boolean;
    get isLoading(): boolean;
    get showLoading(): boolean;
    _routerTransitionDeferred?: RSVP.Deferred<unknown>;
    _routeWillChange(): void;
    _routeDidChange(): void;
    init(): void;
    willDestroy(): void;
    run(...args: any[]): Promise<unknown>;
    _runJob(...args: unknown[]): Promise<unknown>;
    preDelayTask(delay: number): Promise<void>;
    postDelayTask(delay: number): Promise<void>;
}
declare module '@ember/service' {
    interface Registry {
        'loading': LoadingService;
    }
}
