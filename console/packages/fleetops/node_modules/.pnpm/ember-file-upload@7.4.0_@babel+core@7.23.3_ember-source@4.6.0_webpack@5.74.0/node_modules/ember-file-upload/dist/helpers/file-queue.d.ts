import Helper from '@ember/component/helper';
import { UploadFile } from "../upload-file-2bad5ca1.js";
import FileQueueService from "../file-queue-c1330945.js";
import { FileQueueArgs, QueueListener } from "../interfaces-8d97937f.js";
/**
 * `file-queue` helper is one of the core primitives of ember-file-upload.
 *
 * @example
 *
 * using the default queue (no `name` property)
 *
 * ```hbs
 * {{#let (file-queue) as |queue|}}
 *   {{queue.progress}}%
 * {{/let}}
 * ```
 *
 * @example
 *
 * named queue:
 *
 * ```hbs
 * {{#let (file-queue name="photos") as |queue|}}
 *   {{queue.progress}}%
 * {{/let}}
 * ```
 */
declare class FileQueueHelper extends Helper implements QueueListener {
    fileQueue: FileQueueService;
    args: FileQueueArgs;
    compute(_positional: unknown[], args: FileQueueArgs): import("../index.js").Queue;
    onFileAdded(file: UploadFile): void;
    onFileRemoved(file: UploadFile): void;
    onUploadStarted(file: UploadFile): void;
    onUploadSucceeded(file: UploadFile, response: Response): void;
    onUploadFailed(file: UploadFile, response: Response): void;
}
export { FileQueueHelper as default };
